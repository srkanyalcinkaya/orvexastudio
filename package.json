{
  "name": "orvexastudio",
  "version": "1.0.0",
  "description": "Flower e-commerce monorepo",
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api",
    "build": "npm run build -w @orvexa/types && npm run build -w api && npm run build -w web",
    "lint": "npm run lint -w api && npm run lint -w web",
    "test": "npm run test -w api"
  },
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
